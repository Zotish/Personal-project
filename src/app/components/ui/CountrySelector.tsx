// CountrySelector is removed from the UI as per requirements.
// Country detection and mapping provider selection is now 100% automated via background IP & VPN detection.

export interface CountrySelectorProps {
  compact?: boolean;
  className?: string;
}

export function CountrySelector(_props: CountrySelectorProps = {}) {
  return null;
}
